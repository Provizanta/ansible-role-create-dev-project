# {{ ansible_managed }}

# ccache
find_program(CCACHE_UTILITY_EXECUTABLE "ccache")
if(CCACHE_UTILITY_EXECUTABLE)
     set(CMAKE_CXX_COMPILER_LAUNCHER "${CCACHE_UTILITY_EXECUTABLE}")
     set(CMAKE_CUDA_COMPILER_LAUNCHER "${CCACHE_UTILITY_EXECUTABLE}") # CMake 3.9+
endif()
